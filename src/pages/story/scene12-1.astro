---
import InfiniteAnimateScene from "@/layouts/story/InfiniteAnimateScene.astro";
const { cookies } = Astro;

const answer = cookies.get("q13")?.value;
---

<InfiniteAnimateScene text={answer} redirect="/story/scene12-2" />
